---
import { Picture } from "astro:assets";
import heroImage from "assets/HappyCouple2.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-8 pb-8 md:pt-2 md:pb-2">
  <div class="md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Astronaut in the air"
      height={600}
      widths={[200,400,600]}
      sizes="(max-width: 600px) 100vw, 600px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      Tu sueño de casa propia
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      Promovemos el acceso a la vivienda propia, múdate en tan sólo 30 días a la casa de tus sueños.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#contact-section"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
         Contáctanos
      </Link>
    </div>
  </div>
</main>
